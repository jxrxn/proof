{
  "name": "proof",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "build:e2e": "VITE_PROOF_TEST_HOOKS=1 npm run build",
    "e2e": "npm run build:e2e && node test/e2e.mjs",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "hash-wasm": "^4.12.0",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "puppeteer-core": "^24.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.0.0",
    "vitest": "^4.1.10"
  }
}
